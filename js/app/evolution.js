define(function(require) {

    /*
     var BOID_CONSTANTS = {
     min_boid_distance: 25,
     min_boid_speed: 2,
     max_boid_speed: 5,
     boid_nhd: 300,
     delta_speed: 1,
     delta_orientation: 0.1
     };
     */

    var evolution = function(populationSize) {

    };


    return evolution;

});
